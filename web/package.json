{
  "name": "nightwatch-ledger",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "evidence": "node scripts/build-retained-evidence.mjs",
    "dev": "npm run evidence && vite",
    "build": "npm run evidence && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
